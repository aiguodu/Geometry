# MaxArea - 几何最大面积问题交互式课件

交互式几何题目讲解课件，基于 React + TypeScript + Vite 构建。

## 运行

**前置要求:** Node.js 22+

1. 安装依赖:
   `npm install`
2. 复制环境变量示例: `cp .env.example .env.local`
3. 设置你的 `GEMINI_API_KEY` 在 [.env.local](.env.local) 文件中
4. 启动开发服务器:
   `npm run dev`
